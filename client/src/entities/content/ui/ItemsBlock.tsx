import { ContentBaseType } from "@global/types";

interface Props {
    items:ContentBaseType[];
}

const ItemsBlock = ({items}:Props) => {
    return (
        <></>
    )
}

export default ItemsBlock;