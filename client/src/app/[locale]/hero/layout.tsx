import { HeroNavigate } from "@/features/hero";
import { CommonComponentChildren } from "@/types";

export default function HeroLayout({children}:CommonComponentChildren) {
    return (
        <div>
        
        {children}
        </div>)
}
